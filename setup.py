from setuptools import setup

setup(name='tamarack-collector',
      version='0.0.1.dev1',
      description='Tamarack Collector for Python',
      packages=['tamarackcollector'],
      install_requires=['requests'],
      )
